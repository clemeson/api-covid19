<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Barlow&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/css/main.css">
    <link rel="stylesheet" type="text/css" href="/css/responsive.css">
    <title>{{title}}</title>
</head>
<body>
     <header>
            <div class="container-header">
            <figure class="logo"><img class="logo" src="img/logo2.png"></figure>
            <h2>Este serviço tem uma conexão direta com a API do ministério da Saúde</h2>
            <div class="time">
            <p>Ultima Atualização</p>
            <p>{{dados.update}}</p>
            <time></time>
            </div>
            </div>
    </header>
    <main class="container-main">
        <section class="container-return">
            {{#dados}}
                {{#dados.deceasedByRegion}}
                    <div class="info-estado"><h1>Dados do corona virus por estado</h1></div>
                    <section class="return-card">
                    <h1>{{state}}</h1>
                        <div class="return-child">
                            <h1 class="text-input">Obitos: {{count}}</h1> 
                        </div>
                {{/dados.deceasedByRegion}}
                    <div class="return-child">
                            <h1 class="text-input">Infectados: {{count}}</h1>
                        </div>
                        </section>
                        <a href="/" class="btn-clean">Voltar</a>
            {{/dados}}
        </section>
        {{^dados}}
            <section class="container-cards">
                <h1 class="info-cards">Dados do Corona-Virus no Brasil</h1>
                <div class="div-cards">
                    <article class="cards">
                        <figure><img src="img/mask.png" alt=""></figure>
                        <div class="text-cards">
                            <p>Infectados</p>
                            <p>{{dados.infected}}</p>
                        </div>
                    </article>

                    <article class="cards">
                        <figure><img src="img/restart.png" alt=""></figure>
                        <div class="text-cards">
                            <p>Recuperados</p>
                            <p>{{dados.recovered}}</p>
                        </div>
                    </article>

                    <article class="cards">
                        <figure><img src="img/star.png" alt=""></figure>
                        <div class="text-cards">
                        <p>Obitos</p>
                            <p>{{dados.deceased}}</p>
                        </div>
                    </article>
                </div>
            </section>
        {{/dados}}
        <section class="container-getstate">
            <h1 class="info-getstate">Buscar dados do Corona-Virus por Estados</h1>
            <form class="form" method="get">
                <input class="input" name="state" type="text" autofocus placeholder="Digite apenas a sigla do Estado">
                <figure class="img-covid"><img src="img/covid-template.png" alt=""></figure>
                <input type="submit" class="btn-send" value="enviar">
            </form>
        </section>

    </main>

    <script src="/js/script.js"></script>
</body>
</html>